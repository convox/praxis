machine:
  services:
    - docker

dependencies:
  override:
    - go get -t -d -v ./...

test:
  override:
    - pwd
    - echo $GOPATH
    - ls -laR ~/.go_workspace
    - go get github.com/convox/praxis/cmd/cx
    - cx test
