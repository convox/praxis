machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - curl -Ls https://install.convox.com/linux.tgz | tar xz -C /usr/local/bin
    - convox build .

test:
  override:
    - convox start praxis make ci
