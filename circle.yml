machine:
  services:
    - docker

dependencies:
  override:
    - ln -sf /home/ubuntu/.go_workspace/src/$PROJECT $(pwd)
    # - mkdir -p "$GOPATH/src/$PROJECT"
    # - rsync -azC --delete ./ "$GOPATH/src/$PROJECT"

test:
  override:
    - go get ./cmd/cx
    - cx test
