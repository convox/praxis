machine:
  services:
    - docker

dependencies:
  override:
    - curl -Ls https://install.convox.com/linux.tgz | tar xz -C /home/ubuntu/bin
    - convox build .

test:
  override:
    - convox start praxis make ci
