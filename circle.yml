machine:
  environment:
    INSTALL_DIR: /home/ubuntu/.go_workspace/src/github.com/convox/console
    GODIST: "go1.7.5.linux-amd64.tar.gz"
    IMPORT_PATH: "github.com/convox/praxis"

test:
  override:
    - make ci
