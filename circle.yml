machine:
  services:
    - docker

dependencies:
  override:
    - echo $GOPATH
    - ln -sf $GOPATH/src/$PROJECT $(pwd)
    # - mkdir -p "$GOPATH/src/$PROJECT"
    # - rsync -azC --delete ./ "$GOPATH/src/$PROJECT"

test:
  override:
    - go get ./cmd/cx
    - cx test
