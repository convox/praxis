machine:
  environment:
    GOPATH: "/home/ubuntu/go"
    PATH: "$GOPATH/bin:$PATH"
    PROJECT: "github.com/convox/praxis"

checkout:
  post:
    - mkdir -p ${GOPATH}/src/github.com/convox
    - ln -sf . ${GOPATH}/src/github.com/convox/praxis

test:
  override:
    - make test
