balancers:
  rack:
    6443/https: https://rack:3000
services:
  rack:
    build: .
    environment:
      - DEVELOPMENT=false
    test: make check test
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
